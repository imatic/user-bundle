{% if admin.isGranted('ROLES', object) and admin.hasRoute('roles') %}
    <a href="{{ admin.generateObjectUrl('roles', object) }}" class="btn" title="{{ 'Roles'|trans({}, 'ImaticUserBundle') }}">
        <i class="icon-eye-close"></i>
        {{ 'Roles'|trans({}, 'ImaticUserBundle') }}
    </a>
{% endif %}